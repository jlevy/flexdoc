---
name: preamble_only
description: A headingless document (all content is preamble). Pins the documented
  degradation that sections() and toc() are empty while every other view is fully
  populated, and that the serialized DocGraph has no document-layer nodes.
---
This document has no headings at all.
It is just prose, a list, and a quote.

- one plain item
- another item with `code`

> A quote to close, with a [link](https://example.com/preamble).
